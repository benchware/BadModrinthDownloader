If you cannot run it, please make sure you opened PowerShell or Windows Terminal (not command prompt)
If it complains about security, then run the command `set-executionpolicy remotesigned` and pressing "Y".

It's open source so you can inspect the code yourself.
